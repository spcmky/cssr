{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Staff list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Middlename</th>
                <th>Lastname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('staff_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.firstname }}</td>
                <td>{{ entity.middlename }}</td>
                <td>{{ entity.lastname }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('staff_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('staff_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('staff_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
