{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Staff creation</h1>

    <form action="{{ path('staff_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('staff') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
